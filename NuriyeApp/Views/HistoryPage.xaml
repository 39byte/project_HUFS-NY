<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="NuriyeApp.Views.HistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8" Margin="0,0,0,8">
            <TextBlock Text="전체 대여 이력"
                       Style="{StaticResource SubtitleTextBlockStyle}"
                       VerticalAlignment="Center"/>
            <Button Content="새로고침" Command="{x:Bind ViewModel.LoadCommand}"/>
            <ProgressRing IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                          Width="20" Height="20"/>
        </StackPanel>

        <controls:DataGrid
            Grid.Row="1"
            ItemsSource="{x:Bind ViewModel.Rentals, Mode=OneWay}"
            AutoGenerateColumns="False"
            IsReadOnly="True"
            GridLinesVisibility="All"
            AlternatingRowBackground="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <controls:DataGridTextColumn Header="신청자" Binding="{Binding Applicant}" Width="80"/>
                <controls:DataGridTextColumn Header="연락처" Binding="{Binding Contact}" Width="110"/>
                <controls:DataGridTextColumn Header="장비명" Binding="{Binding Equipment}" Width="200"/>
                <controls:DataGridTextColumn Header="대여시작일" Binding="{Binding StartDate}" Width="100"/>
                <controls:DataGridTextColumn Header="반납예정일" Binding="{Binding EndDate}" Width="100"/>
                <controls:DataGridTextColumn Header="담당자" Binding="{Binding Staff}" Width="130"/>
                <controls:DataGridTextColumn Header="상태" Binding="{Binding Status}" Width="80"/>
                <controls:DataGridTextColumn Header="비고" Binding="{Binding Remarks}" Width="150"/>
                <controls:DataGridTextColumn Header="실제반납일" Binding="{Binding ActualReturnDate}" Width="110"/>
                <controls:DataGridTextColumn Header="액세서리" Binding="{Binding Accessories}" Width="150"/>
                <controls:DataGridTextColumn Header="신청일시" Binding="{Binding SubmittedAt}" Width="130"/>
            </controls:DataGrid.Columns>
        </controls:DataGrid>

        <TextBlock Grid.Row="2"
                   Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"
                   Margin="0,8,0,0"/>
    </Grid>
</Page>

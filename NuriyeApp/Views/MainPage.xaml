<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="NuriyeApp.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NuriyeApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title bar area -->
        <Grid Grid.Row="0" Height="48" Padding="16,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="&#xB204;&#xB9AC;&#xC608; &#xCE74;&#xBA54;&#xB77C; &#xB300;&#xC5EC; &#xAD00;&#xB9AC;"
                       Style="{StaticResource SubtitleTextBlockStyle}"
                       VerticalAlignment="Center"/>
            <Button Grid.Column="1" Content="&#xB85C;&#xADF8;&#xC544;&#xC6C3;"
                    Click="LogoutButton_Click"
                    VerticalAlignment="Center"
                    Style="{StaticResource NuriyeDefaultButtonStyle}"/>
        </Grid>

        <!-- Tab content -->
        <TabView Grid.Row="1"
                 x:Name="MainTabView"
                 TabWidthMode="Equal"
                 IsAddTabButtonVisible="False"
                 SelectionChanged="MainTabView_SelectionChanged">

            <TabViewItem IsClosable="False">
                <TabViewItem.Header>
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <TextBlock Text="&#xC2B9;&#xC778; &#xB300;&#xAE30;" VerticalAlignment="Center"/>
                        <Border x:Name="PendingBadge"
                                Background="#EF5350"
                                CornerRadius="10"
                                MinWidth="20" Height="20"
                                Padding="5,0"
                                VerticalAlignment="Center"
                                Visibility="Collapsed">
                            <TextBlock x:Name="PendingBadgeText"
                                       Foreground="White"
                                       FontSize="11"
                                       FontWeight="Bold"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>
                        </Border>
                    </StackPanel>
                </TabViewItem.Header>
                <local:PendingPage x:Name="PendingPageControl"/>
            </TabViewItem>

            <TabViewItem Header="&#xC9C4;&#xD589; &#xC911; &#xB300;&#xC5EC;" IsClosable="False">
                <local:OngoingPage x:Name="OngoingPageControl"/>
            </TabViewItem>

            <TabViewItem Header="&#xC804;&#xCCB4; &#xC774;&#xB825;" IsClosable="False">
                <local:HistoryPage x:Name="HistoryPageControl"/>
            </TabViewItem>

            <TabViewItem Header="&#xC7A5;&#xBE44; &#xBAA9;&#xB85D;" IsClosable="False">
                <local:InventoryPage x:Name="InventoryPageControl"/>
            </TabViewItem>

            <TabViewItem Header="&#xC124;&#xC815;" IsClosable="False">
                <local:SettingsPage x:Name="SettingsPageControl"/>
            </TabViewItem>
        </TabView>
    </Grid>
</Page>

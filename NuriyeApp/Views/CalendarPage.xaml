<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="NuriyeApp.Views.CalendarPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Month Navigation -->
        <Grid Grid.Row="0" Padding="12,8" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="&#x25C0; &#xC774;&#xC804;&#xB2EC;"
                    Command="{x:Bind ViewModel.PrevMonthCommand}"
                    Style="{StaticResource NuriyeDefaultButtonStyle}"/>
            <TextBlock Grid.Column="1"
                       Text="{x:Bind ViewModel.MonthTitle, Mode=OneWay}"
                       Style="{StaticResource SubtitleTextBlockStyle}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
            <Button Grid.Column="2" Content="&#xB2E4;&#xC74C;&#xB2EC; &#x25B6;"
                    Command="{x:Bind ViewModel.NextMonthCommand}"
                    Style="{StaticResource NuriyeDefaultButtonStyle}"/>
        </Grid>

        <!-- Loading -->
        <ProgressBar Grid.Row="1"
                     IsIndeterminate="True"
                     Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>

        <!-- Day Headers -->
        <Border Grid.Row="2" Background="{ThemeResource NuriyeSurfaceAltBrush}"
                CornerRadius="8,8,0,0" Margin="12,4,12,0">
            <Grid x:Name="DayHeaderGrid" Padding="0,4"/>
        </Border>

        <!-- Calendar Body wrapped in Card -->
        <Border Grid.Row="3"
                Background="{ThemeResource NuriyeSurfaceBrush}"
                BorderBrush="{ThemeResource NuriyeBorderBrush}"
                BorderThickness="1"
                CornerRadius="0,0,12,12"
                Margin="12,0,12,12">
            <ScrollViewer Padding="0">
                <Grid x:Name="CalendarBodyGrid"/>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>
